import ShareModal from '.';

import type { Meta, StoryObj } from '@storybook/react';

export default {
  title: 'ShareModal',
  component: ShareModal,
} as Meta<typeof ShareModal>;

type Story = StoryObj<typeof ShareModal>;

export const Primary: Story = {};
