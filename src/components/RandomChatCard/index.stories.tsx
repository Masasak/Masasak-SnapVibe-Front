import RandomChatCard from '.';

import type { Meta, StoryObj } from '@storybook/react';

export default {
  title: 'RandomChatCard',
  component: RandomChatCard,
} as Meta<typeof RandomChatCard>;

type Story = StoryObj<typeof RandomChatCard>;

export const Primary: Story = {};
